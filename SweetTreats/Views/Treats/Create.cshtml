@{
  Layout = "_Layout";
}

@model SweetTreat.Models.Treat

<h3>Add a new Treat</h3>

@using (Html.BeginForm())
{
  <div class="form-row">
    @Html.LabelFor(model => model.Title) @Html.TextBoxFor(model => model.Title)
    @Html.LabelFor(model => model.Copies) @Html.EditorFor(model => model.Copies)
  </div>
  
  <button class="green" type="submit">Add new treat</button>
}

<div class="button-footer">
  <hr />
  <a href='@Url.Action("Index")'>
    <button>Show all treats</button>
  </a>
</div>